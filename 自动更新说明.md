# 交易所库存自动更新（路线 A）· 使用说明

> 目标：**网页打开即显示最新交易所库存**，无需你反复上传整页。

## 一、工作原理

```
GitHub Actions（每交易日 08:30 北京时间）
        ↓  运行 scripts/fetch_stocks.py
   抓 LME 库存 + SHFE 每日仓单（均为公开页面）
        ↓  写入 data/stocks.json 并自动 commit
   Pages 自动重建
        ↓
你打开页面 → 同源读取 data/stocks.json → 库存卡片自动刷新
```

- 页面里的库存卡片带「**数据时点**」标注，显示本次数据的截止日。
- 若抓取失败 / 本地打开文件 / 断网，页面**自动回退到内置基线**，其余功能不受影响。

## 二、覆盖范围

| 交易所 | 品种 | 单位 | 来源（公开） |
|--------|------|------|--------------|
| LME | Cu / Al / Zn / Pb / Ni / Sn | kt（原表 t÷1000） | westmetall 公开镜像表（LME 官网对本类请求返回 403） |
| SHFE | Cu / Al / Zn / Pb / Ni / Sn | kt（仓单合计 t÷1000） | 上期所官网「每日仓单」公布页 |

**合规红线**：只抓交易所公开数据；**不抓取任何会员制 / 付费页面（如 SMM 会员内容）**。

## 三、首次启用（一次性）

1. 把本仓库 zip 解压后**覆盖到仓库根**，确认根目录有：
   ```
   index.html
   data/stocks.json
   scripts/fetch_stocks.py
   .github/workflows/update-stocks.yml
   ```
2. 打开仓库 **Settings → Actions → General → Workflow permissions**
   → 选择 **Read and write permissions** → Save  
   （否则工作流无法把 `data/stocks.json` 提交回来）
3. 打开 **Actions** 标签页 → 左侧选 `Update exchange stocks` → **Run workflow** 手动跑一次，确认成功。
4. 打开 **Settings → Pages** → Source 选 **Deploy from a branch** → 分支 `main`，目录 **`/ (root)`** → Save。

## 四、日常

- **什么都不用做**：工作日早晨自动更新库存并上线。
- 想立刻刷新：Actions 页面点 **Run workflow**。
- 想改抓取范围/字段：改 `scripts/fetch_stocks.py`，提交即可。

## 五、常见问题

| 现象 | 原因 / 处理 |
|------|-------------|
| 库存数字没变 | 当天为非交易日，或交易所尚未公布；看「数据时点」是否推进 |
| 页面显示「内置基线」 | JSON 未加载成功：确认 `data/stocks.json` 在仓库根下；强刷页面 |
| Actions 报 permission denied | 第三节第 2 步的写权限没开 |
| SHFE 无当日数据 | 仓单通常盘后发布；脚本会自动向前回退最多 10 天找最新公布日 |

## 六、时点口径说明

- 「as_of」= 数据所属交易日（LME 与 SHFE 取两者较新者）。
- LME 与 SHFE 公布时间不同，同一 as_of 下两者可能来自不同交易日，卡片内各自标注具体日期，**不合并为同一时点**。
